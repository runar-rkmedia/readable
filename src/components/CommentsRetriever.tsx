import * as React from 'react'
import List from 'material-ui/List'
import { PostI } from '../interfaces'
import { } from './'

export const CommentsRetriever = (props: {
  post: PostI
}) => {
  return (
    <List>
      Loading commments.
    </List>
  )
}
